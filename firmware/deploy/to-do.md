# TO-DO LIST en el firmware

- Comprobar si Claude-code puede recrear el firmware para trixie.
- **[e-ink]** Ajustar el renderizado de texto en la pantalla e-ink para premisas largas: el texto se sale de la pantalla y queda cortado. Revisar el cálculo de tamaño de fuente dinámico o el wrap de texto en `eink.py` / `modules/eink.py`.
